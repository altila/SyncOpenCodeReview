

================================================================================
===== 执行批次：2026-05-10 00:00:57 =====
================================================================================

# qKnow 代码更新分析 - 2026-05-10

> 生成时间: 2026-05-10 00:00:57

---

你好！我是你的架构师和代码审查专家。针对 qKnow 仓库最新的代码提交记录（涉及数据库调整、结构化/非结构化抽取优化及 LLM 切换支持），我进行了深度的代码分析。

以下是本次代码迭代的详细报告：

---模块分隔：[基础配置]---

### 1. 迭代功能清单

| 功能名称 | 功能描述 | 标注 | 相关文件路径 |
| :--- | :--- | :--- | :--- |
| 数据库类型限制 | 在 `DbType` 枚举中增加逻辑，明确仅支持 MYSQL、ORACLE 和 ORACLE_12C，增强系统稳定性。 | `[优化]` | `qknow-framework/qknow-common/src/main/java/tech/qiantong/qknow/common/database/constants/DbType.java` |
| 依赖项补充 | 在业务模块中引入知识库(kb)、数据管理(dm)和人工智能(ai)的 API 依赖，为大模型抽取功能打下基础。 | `[新增]` | `qknow-module-ext/qknow-module-ext-biz/pom.xml` |
| 网络请求优化 | 将前端全局请求超时时间从 10s 延长至 30s，以适配大模型推理较长的响应时间。 | `[优化]` | `qknow-ui/src/utils/request.js` |
| 索引文件更新 | 更新了 Lucene 索引目录下的段文件（二进制文件变更）。 | `[优化]` | `indexDir/` 目录下多个文件 |

---模块分隔：[抽取引擎]---

### 1. 迭代功能清单

| 功能名称 | 功能描述 | 标注 | 相关文件路径 |
| :--- | :--- | :--- | :--- |
| LLM 抽取支持 | 实现 `execTaskByModel` 方法，支持通过大模型工作流进行三元组抽取，包含提示词生成、分段处理及结果解析。 | `[新增]` | `qknow-module-ext/qknow-module-ext-biz/src/main/java/tech/qiantong/qknow/module/ext/service/extUnstructTask/impl/ExtUnstructTaskServiceImpl.java` |
| DeepKE 脚本优化 | 新增 Windows 批处理脚本 `start.cmd`，并修正了 `start.sh` 中的 Docker 容器 ID 硬编码问题。 | `[修复]` | `bin/DeepKE/start.cmd`<br>`bin/DeepKE/start.sh` |
| 跨平台兼容性 | 优化 `DeepkeExtractionServiceImpl`，根据操作系统自动选择执行 `.cmd` 或 `.sh` 脚本。 | `[优化]` | `qknow-module-ext/qknow-module-ext-biz/src/main/java/tech/qiantong/qknow/module/ext/service/deepke/impl/DeepkeExtractionServiceImpl.java` |
| 抽取模式切换 | 配置文件支持在 `deepke` 和 `model` (LLM) 抽取模式之间切换，默认改为 `model`。 | `[优化]` | `qknow-module-ext/qknow-module-ext-biz/src/main/resources/application-ext-dev.yml`<br>`qknow-module-ext/qknow-module-ext-biz/src/main/resources/application-ext-prod.yml` |

---模块分隔：[数据持久]---

### 1. 迭代功能清单

| 功能名称 | 功能描述 | 标注 | 相关文件路径 |
| :--- | :--- | :--- | :--- |
| 抽取模型扩展 | `ExtExtractionDO` 增加 `startSchemaId`、`startTextIds` 等字段，支持记录实体在原文中的位置及 Schema 关联。 | `[新增]` | `qknow-module-ext/qknow-module-ext-biz/src/main/java/tech/qiantong/qknow/module/ext/dal/dataobject/extraction/ExtExtractionDO.java` |
| 属性类型枚举 | 新增 `AttributeDataTypeEnums`，定义了文本、整数、布尔等 7 种概念属性数据类型。 | `[新增]` | `qknow-module-ext/qknow-module-ext-biz/src/main/java/tech/qiantong/qknow/module/ext/extEnum/AttributeDataTypeEnums.java` |
| Neo4j 写入优化 | 重构 `insertExtractionList`，增加对孤立节点（无尾节点或无关系）的兼容处理，并支持 Schema ID 写入。 | `[优化]` | `qknow-module-ext/qknow-module-ext-biz/src/main/java/tech/qiantong/qknow/module/ext/service/neo4j/service/impl/ExtNeo4jServiceImpl.java` |

---模块分隔：[前端交互]---

### 1. 迭代功能清单

| 功能名称 | 功能描述 | 标注 | 相关文件路径 |
| :--- | :--- | :--- | :--- |
| 路由结构重构 | 将知识抽取相关的路由从 `/ext` 迁移至 `/kg/ext`，统一归口到知识图谱模块下。 | `[优化]` | `qknow-ui/src/router/ext/public/index.js`<br>`qknow-ui/src/router/index.js`<br>`qknow-ui/src/router/kg/dynamic/index.js` |
| 溯源功能增强 | 图谱探索页面支持根据 `textIds` 更加精准地高亮显示原文片段。 | `[优化]` | `qknow-ui/src/views/app/graphExploration/index.vue` |
| 概念属性管理 | 优化概念属性详情页的 UI 布局，增加查询表单及操作按钮的交互体验。 | `[优化]` | `qknow-ui/src/views/ext/extSchema/detail/index.vue` |

---

### 2. 迭代功能总结

*   **核心功能概述**：
    本次更新实现了知识抽取引擎的重大升级，核心是引入了**基于大语言模型（LLM）的非结构化抽取能力**。系统现在支持在传统的 DeepKE（小模型/规则）与 LLM 之间灵活切换。同时，对底层数据模型进行了扩展，支持抽取结果与原文片段（Text IDs）及图谱模式（Schema）的深度绑定。
*   **修复的 Bug 汇总**：
    1.  修复了 DeepKE 脚本在 Windows 环境下无法运行的问题。
    2.  修正了 Docker 容器 ID 硬编码导致的执行失败。
    3.  解决了 Neo4j 写入时若尾节点为空会导致整条记录丢失的逻辑缺陷。
*   **整体影响范围评估**：
    *   **后端**：抽取逻辑发生较大变化，引入了多线程异步处理大模型任务，需关注服务器内存与线程池压力。
    *   **前端**：路由路径变更，旧的 URL 书签将失效；UI 交互上增强了图谱与原文的溯源关联。
    *   **数据**：Neo4j 节点属性增多，旧数据可能需要清洗以适配新的 Schema 关联功能。

---

### 3. 优劣与风险分析

#### **优势/亮点**：
1.  **架构灵活性**：通过策略模式的思想（虽然目前是用 if-else 实现，但配置化已完成），实现了 DeepKE 与 LLM 的可切换性，适应不同算力场景。
2.  **异步性能优化**：在 `ExtUnstructTaskServiceImpl` 中使用了 `CompletableFuture` 并发处理文档分段，显著提升了大模型抽取的吞吐量。
3.  **数据闭环**：增加了 `startTextIds` 等字段，实现了从“图谱实体”到“原文片段”的精准溯源，这是知识图谱应用中的关键能力。
4.  **健壮性增强**：Neo4j 写入逻辑加入了非空校验和对象复用保护，减少了因脏数据导致的写入异常。

#### **劣势/风险**：
1.  **硬编码风险**：
    *   `ExtUnstructTaskServiceImpl` 中仍存在 `base + kgDocument.getPath()` 拼接路径的逻辑，且依赖本地物理路径前缀 `prefix`，在分布式部署环境下可能会失效。
    *   `DeepkeExtractionServiceImpl` 中的 Docker 容器 ID 虽然更新了，但依然是硬编码，建议提取到配置文件中。
2.  **资源管理隐患**：
    *   在 `execTaskByModel` 方法中，`executor.shutdown()` 的触发条件（`getCurrentConcurrentCount() <= 0`）在高并发场景下可能存在竞态条件，导致线程池提前关闭或无法关闭。
3.  **代码冗余**：
    *   `ExtUnstructTaskServiceImpl` 中存在大量注释掉的代码块（如旧的 URL 拼接逻辑），建议清理以保持代码整洁。
    *   `cover2GraphProperties` 方法目前返回了空 Map，大模型抽取的属性解析逻辑尚未完全实现。
4.  **前端硬编码**：
    *   `graphExploration/index.vue` 中存在硬编码的 `pageType` 判断（"1", "2", "3"），建议使用常量或枚举维护。
5.  **安全隐患**：
    *   `text.replace("'", "").replace("\"", "")` 这种简单的字符过滤不足以完全防御复杂的 Prompt 注入攻击，建议对 LLM 的输入输出进行更严格的合规性校验。

**专家建议**：下一步应重点完善大模型抽取结果中的属性解析逻辑（`cover2GraphProperties`），并将所有环境相关的配置（如 Docker ID、文件存储根路径）统一收拢到 Nacos 或配置文件中。

---

*本报告由 SyncOpenCodeReview 自动生成*
