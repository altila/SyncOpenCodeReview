

================================================================================
===== 执行批次：2026-04-29 13:33:29 =====
================================================================================

# WeKnora 代码更新分析 - 2026-04-29

> 生成时间: 2026-04-29 13:33:29

---

你好，我是架构师和代码审查专家。针对 WeKnora 仓库从 v0.4.0 到 v0.5.0 的重大版本迭代，我进行了深度的代码审查和逻辑分析。

以下是本次迭代的代码迭代报告：

### 1. 迭代功能清单

---模块分隔：[租户与安全]---

| 功能名称 | 功能描述 | 相关文件路径 |
| :--- | :--- | :--- |
| [新增] API Key 重置 | 在 API 信息页面暴露 API Key 重置功能，增强安全性 | `feat(tenant): expose API Key reset` |
| [新增] 客户端租户切换 | Go 客户端支持通过 `WithTenantID` 或 Context 动态切换 `X-Tenant-ID` 请求头 | `client/client.go`, `client/example.go` |
| [修复] API Key 返回 | 创建或重置 API Key 后返回明文，确保用户能及时保存 | `fix(tenant): return plaintext API key` |
| [修复] Helm 升级配置 | 在 Helm 升级过程中保留 `SYSTEM_AES_KEY` 和 `TENANT_AES_KEY`，防止加密数据失效 | `fix(helm): preserve AES_KEY` |

---模块分隔：[存储与解析]---

| 功能名称 | 功能描述 | 相关文件路径 |
| :--- | :--- | :--- |
| [新增] 存储白名单 | 引入 `STORAGE_ALLOW_LIST` 环境变量，限制用户可使用的文件存储类型 | `.env.example`, `feat: add STORAGE_ALLOW_LIST` |
| [优化] DOCX 解析限制 | 将 DOCX 默认解析页数从 100 页改为不限制（0），支持超长文档解析 | `.env.example`, `docreader/config.py`, `docreader/parser/docx_parser.py` |
| [修复] 图片过滤逻辑 | 修复 docparser 在过滤图标时误删独立上传图片的问题 | `fix(docparser): preserve standalone images` |

---模块分隔：[知识库检索]---

| 功能名称 | 功能描述 | 相关文件路径 |
| :--- | :--- | :--- |
| [新增] 文档批量操作 | 知识库文档列表支持批量删除及列表视图优化 | `feat(knowledge-base): add document batch delete` |
| [优化] 兜底回答增强 | 当搜索无结果时，在 Fallback Prompt 中注入知识库文档列表 `{{kb_documents}}`，引导用户了解库内内容 | `config/prompt_templates/fallback.yaml` |
| [优化] 查询重写逻辑 | 增强对“列出所有文档”、“整理数据”等宽泛意图的识别，防止关键词被过度精简 | `config/prompt_templates/rewrite.yaml` |
| [修复] 检索排序对齐 | 统一 `Execute` 和 `rerankResults` 之间的重排序优先级逻辑 | `fix(search): align rerank priority` |

---模块分隔：[智能体与IM]---

| 功能名称 | 功能描述 | 相关文件路径 |
| :--- | :--- | :--- |
| [新增] IM 布局重构 | IM 渠道列表改为响应式网格卡片，表单由弹窗改为抽屉式，提升 UX | `da90867d - feat(ui): refactor IM channels` |
| [新增] 超时配置 | 前端支持配置 LLM 调用超时时间，适应长文本生成场景 | `feat: Add LLM call timeout configuration` |
| [优化] IM 适配器重构 | 将不同平台的适配器工厂移动到独立的子包中，解耦架构 | `refactor(im): move adapter factories` |
| [修复] 会话隔离 | 实现 IM 会话按 Agent 隔离，并支持从已删除会话中自动恢复 | `fix(im): isolate IM sessions` |
| [修复] 推理中断问题 | 移除 Pipeline 级别的超时限制，防止多轮 Agent 推理被误杀 | `fix(im): remove pipeline-level timeout` |

---模块分隔：[Wiki与集成]---

| 功能名称 | 功能描述 | 相关文件路径 |
| :--- | :--- | :--- |
| [新增] Wiki 治理 API | 新增 Wiki 链接重构、健康检查 (Lint)、统计 (Stats) 及图谱 (Graph) 等 API | `docs/docs.go` (Swagger 更新) |
| [新增] 微信小程序 | 增加微信小程序插件支持 | `feat(miniprogram): add WeChat mini program plugin` |
| [优化] Wiki 摄入稳定性 | 引入摄入锁、失败操作重入队列及同步任务重试机制，解决 Redis 异常导致的丢数问题 | `fix(wiki): Lite ingest lock, failed-op requeue` |

---

### 2. 迭代功能总结

*   **核心功能概述**：
    本次 v0.5.0 版本是一个承上启下的重要更新。核心聚焦于**多租户架构的完善**（客户端支持租户切换）、**Wiki 知识库的工业级稳定性增强**（重试与锁机制）以及**RAG 检索体验的精细化优化**（Fallback 注入文档列表）。同时，项目在**多端接入**（微信小程序）和**UI/UX**（IM 渠道重构）上有了显著提升。

*   **修复的 Bug 汇总**：
    修复了 Helm 升级导致密钥丢失的致命风险；解决了 IM 会话在多 Agent 场景下的混淆问题；修正了长文档解析被截断的限制；并对前端知识库列表的交互细节（粘性表头、悬浮工具栏）进行了大量打磨。

*   **整体影响范围评估**：
    *   **后端**：API 结构有较大变动（Swagger 显示大量定义重构），Wiki 模块逻辑变得更加复杂但健壮。
    *   **前端**：IM 模块和知识库管理模块的交互逻辑发生了变化，用户需要适应新的抽屉式操作。
    *   **运维**：引入了新的环境变量 `STORAGE_ALLOW_LIST`，升级时需注意 AES Key 的持久化。

---

### 3. 优劣与风险分析

#### **优势/亮点**：
1.  **RAG 策略进化**：在 `fallback.yaml` 中引入 `{{kb_documents}}` 是一个非常聪明的做法。当向量检索失效时，通过展示文档列表给 LLM，可以让 AI 告诉用户“我没找到具体答案，但库里有这些文档，你是否想问相关的？”，极大地提升了用户体验。
2.  **架构解耦**：`refactor(im)` 将适配器工厂按平台拆分，符合“开闭原则”，未来接入钉钉、飞书等新平台时，代码冲突风险更低。
3.  **多租户增强**：Go 客户端对 `X-Tenant-ID` 的处理兼顾了全局配置和请求级覆盖，逻辑严密，支持复杂的服务端集成场景。
4.  **Wiki 健壮性**：针对 Redis 异常和 JSON 解析失败导致的“静默丢数”进行了专项修复，这是系统走向生产环境的必要改进。

#### **劣势/风险**：
1.  **资源消耗风险**：`docreader` 移除了 DOCX 100 页的默认限制（改为 100,000 页）。如果用户上传超大文档，可能会导致解析节点 OOM 或长时间占用计算资源，建议在生产环境通过环境变量设置一个合理的上限（如 500 页）。
2.  **Context 键名硬编码**：在 `client/client.go` 中，从 Context 获取 `TenantID` 使用了字符串常量 `"TenantID"`。在 Go 最佳实践中，应使用**自定义类型**作为 Context 的 Key，以避免与其他中间件或库产生命名冲突。
3.  **API 兼容性**：从 `docs/docs.go` 的差异看，错误定义（`AppError`）的引用路径发生了变化。如果外部有直接依赖 Swagger 生成代码的调用方，可能会面临破坏性变更（Breaking Change）。
4.  **配置项复杂度**：随着 `STORAGE_ALLOW_LIST` 等配置项增多，`.env` 文件愈发臃肿，建议后续考虑引入配置中心或分层配置管理。

**建议**：在 v0.5.x 的后续微调中，重点关注超大文档解析时的内存监控，并优化 Go 客户端 Context Key 的定义方式。

---

*本报告由 SyncOpenCodeReview 自动生成*
