

================================================================================
===== 执行批次：2026-04-29 23:10:22 =====
================================================================================

# LangChain 模型集成 模块更新分析 - 2026-04-29

> 生成时间: 2026-04-29 23:10:22
> 所属版本: 2026-04-29

---

你好，我是代码审查专家。基于你提供的 LangChain 仓库最新提交记录和代码差异，我整理了这份详细的代码迭代报告。

## 模型集成

| 功能名称 | 类型 | 功能描述 | 相关文件路径 |
| :--- | :--- | :--- | :--- |
| **Perplexity 嵌入支持** | [新增] | 新增 `PerplexityEmbeddings` 类，支持 Perplexity AI 的嵌入模型（如 `pplx-embed-v1-4b`），并处理了其特有的 `base64_int8` 编码解码。 | `libs/partners/perplexity/langchain_perplexity/embeddings.py`<br>`libs/partners/perplexity/langchain_perplexity/__init__.py` |
| **Anthropic 缓存增强** | [修复] | 针对非直接 API 调用（如通过 Bedrock 使用 Anthropic）优化了 `cache_control`。逻辑改为将缓存断点注入到最后一个符合条件的块中，并自动跳过 Anthropic 不允许设置断点的 `code_execution` 相关块。 | `libs/partners/anthropic/langchain_anthropic/chat_models.py`<br>`libs/partners/anthropic/tests/unit_tests/test_chat_models.py` |
| **HTTPX 资源安全释放** | [修复] | 在 Anthropic 客户端包装器中增加了对 `__del__` 析构函数的保护，防止在对象未完全初始化或已关闭时调用 `close()` 抛出异常。 | `libs/partners/anthropic/langchain_anthropic/_client_utils.py` |
| **Perplexity 流式优化** | [优化] | 优化了 `ChatPerplexity` 的流式输出，确保即使是仅包含 Usage（用量）信息的空 Chunk 也能被 yield，以便下游实时获取 Token 统计。 | `libs/partners/perplexity/langchain_perplexity/chat_models.py` |
| **模型配置更新** | [优化] | 更新了 DeepSeek、OpenAI (GPT-5.5 Pro)、Gemini 3.1 等最新模型的 Token 限制、功能支持（如推理输出、工具调用）等元数据。 | `libs/partners/*/data/_profiles.py` |

---

*本报告由 SyncOpenCodeReview 自动生成*
